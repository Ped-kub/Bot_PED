<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bot Control Panel</title>
    <style>
        body { font-family: sans-serif; background: #2c3e50; color: white; padding: 20px; }
        .container { max-width: 600px; margin: 0 auto; background: #34495e; padding: 20px; border-radius: 10px; }
        input, button { width: 100%; padding: 10px; margin: 10px 0; border-radius: 5px; border: none; }
        button { background: #2ecc71; color: white; cursor: pointer; font-weight: bold; }
        button:hover { background: #27ae60; }
        .logout-btn { background: #e74c3c; margin-top: 20px; }
        .logout-btn:hover { background: #c0392b; }
        .profile { display: flex; align-items: center; gap: 10px; margin-bottom: 20px; border-bottom: 1px solid #7f8c8d; padding-bottom: 10px; }
        .profile img { border-radius: 50%; width: 50px; }
        .success { color: #2ecc71; }
        .error { color: #e74c3c; }
    </style>
</head>
<body>
    <div class="container">
        <div class="profile">
            <% if (user.avatar) { %>
                <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="Profile">
            <% } %>
            <div>
                <h3>สวัสดี, <%= user.username %></h3>
                <small>Admin Panel</small>
            </div>
        </div>

        <h1>🎛️ แผงควบคุมบอท: <%= botName %></h1>
        <hr>

        <h2>💰 เติมแต้มพิเศษ</h2>
        <% if (locals.message) { %>
            <p class="<%= status === 'success' ? 'success' : 'error' %>"><%= message %></p>
        <% } %>

        <form action="/add-points" method="POST">
            <label>Discord User ID (ลูกค้า):</label>
            <input type="text" name="targetId" placeholder="เช่น 946050..." required>

            <label>จำนวนแต้ม:</label>
            <input type="number" name="amount" placeholder="เช่น 100" required>

            <button type="submit">ยืนยันการเติมแต้ม</button>
        </form>

        <a href="/logout"><button class="logout-btn">🚪 ออกจากระบบ</button></a>
    </div>
</body>
</html>